<view wx:if="{{!src}}" class="page">
  <view class="emblem">
    <view class="bar"></view>
    <view class="bar"></view>
  </view>
  <text class="title">二</text>
  <text class="hint">{{message}}</text>
</view>
<web-view wx:if="{{src}}" src="{{src}}"></web-view>
